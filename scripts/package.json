{
  "name": "@rs-planner/scripts",
  "version": "1.0.0",
  "description": "Data synchronization scripts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "dev": "tsc --watch",
    "fetch-recipes": "node dist/fetch-recipes.js"
  },
  "dependencies": {
    "@rs-planner/core": "1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
